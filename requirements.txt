# No third-party packages required.
# Windows audio uses the built-in MCI interface via ctypes (standard library).
# On Linux the watcher shells out to ffplay / mpg123 / mpv if you ever run it there.
